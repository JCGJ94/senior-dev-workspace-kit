{
  "name": "@pedrito/workspace",
  "private": true,
  "version": "4.0.0",
  "workspaces": ["packages/*"],
  "engines": {
    "bun": ">=1.1.0"
  },
  "scripts": {
    "build": "bun run --filter '*' build",
    "test": "bun run --filter '*' test",
    "typecheck": "bun run --filter '*' typecheck"
  },
  "devDependencies": {
    "typescript": "^5.4.0"
  }
}
