# Pedrito Kit

Governance kit for AI agents: core rules, skills, workflows, SDD specs, Engram memory.

This directory is the V3 source kit — installed globally via `setup.sh` at the repo root.
See the root `README.md` for the full V4 monorepo overview.
